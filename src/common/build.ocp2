
OCaml.library("data-encoding", ocaml + {
  files = ["data_encoding.ml"];
  requires = ["ocplib-json-typed"; "yojson"; "timeline-data"]
});

OCaml.library("xhr-lwt", ocaml+ {
  requires = [
    "ez-api-js";
    "js_of_ocaml-lwt";
    "yojson";
  ] ;
  files = [
    "xhr_lwt.ml";
    ];
});

OCaml.library("timeline-data", ocaml + {
  files = [
   	 "data_types.ml";
   	 "utils.ml";];
  requires = [
    "calendar";
  ] ;
});

OCaml.library("config", ocaml + {files = ["config.ml"]});